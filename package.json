{
  "name": "imagetools-app",
  "version": "1.0.0",
  "description": "React frontend + Express backend for imagetools.shop",
  "type": "module",
  "main": "backend-robot/dist/server.js",
  "scripts": {
    "install-all": "npm install && cd backend-robot && npm install && cd ../mr-robot && npm install && cd ..",
    "build": "cd backend-robot && npm install && npm run build && cd ../mr-robot && npm install && npm run build && cd ..",
    "start": "node backend-robot/dist/server.js",
    "dev": "npm --prefix backend-robot run dev & npm --prefix mr-robot run dev",
    "dev:backend": "npm --prefix backend-robot run dev",
    "dev:frontend": "npm --prefix mr-robot run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "@types/pg": "^8.16.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "18.x || 20.x"
  }
}
